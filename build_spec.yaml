version: 0.1
component: build
timeoutInSeconds: 1000
shell: bash

steps:
  - type: Command
    name: "Run Build"
    command: |
      mvn clean install -DskipTests
      git clone https://devops.scmservice.uk-london-1.oci.oraclecloud.com/namespaces/lrcl06h6pu7e/projects/GHA-PoC/repositories/build_specs
      ls
